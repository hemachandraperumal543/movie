[build]
builder = "NIXPACKS"
buildCommand = "cd backend && mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar backend/target/backend-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/feedback"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
